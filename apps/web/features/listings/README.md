# features/listings

Código de frontend do módulo **Anúncios**: api client, hooks TanStack Query, tabela de anúncios com colunas configuráveis, vínculo com SKU (Estoque).

Espelha o módulo backend `apps/api/src/listings`. Sem telas implementadas ainda.

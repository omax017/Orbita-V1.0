# features/discovery

Código de frontend do módulo **Descobrir** (pesquisa de mercado/concorrentes, histórico de buscas). Espelha o módulo backend `apps/api/src/discovery`. Sem telas implementadas ainda.

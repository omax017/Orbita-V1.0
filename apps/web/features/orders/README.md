# features/orders

Código de frontend do módulo **Pedidos**: api client (`api.ts`), hooks TanStack Query (`hooks.ts`), tipos de tela e componentes específicos (tabela de pedidos, drawer de detalhe/DRE por pedido).

Espelha o módulo backend `apps/api/src/orders`. Sem telas implementadas ainda — isso entra numa etapa futura.

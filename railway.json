{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @hubwin/api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @hubwin/api exec prisma migrate deploy && pnpm --filter @hubwin/api start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
